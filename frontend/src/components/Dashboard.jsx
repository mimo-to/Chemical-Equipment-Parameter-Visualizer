import Upload from './Upload';

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <p>Welcome to the protected dashboard.</p>
            <Upload />
        </div>
    );
};

export default Dashboard;
